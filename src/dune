(library
 (name dblConfig)
 (modules dblConfig)
 (libraries unix dune-glob))

(executable
  (name dbl)
  (modes byte exe)
  (public_name dbl)
  (libraries interpLib dblParser typeInference effectInference toCore toCPS 
             cps eval unix dune-glob)
  (modules dbl pipeline conETypeErase coreTypeErase))
